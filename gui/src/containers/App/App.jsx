import React from 'react';
// import PropTypes from 'prop-types';
import styles from './styles.css';

const App = (/* { children } */) => (
  <div>
    {/* // {children} */}
    <h1 className={styles.title}>yeahhh</h1>
  </div>
);

// App.propTypes = {
//   children: PropTypes.element.isRequired,
// };

export default App;
